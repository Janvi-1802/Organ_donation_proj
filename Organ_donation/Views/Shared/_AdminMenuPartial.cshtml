@using Microsoft.AspNetCore.Identity


@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager

@if (SignInManager.IsSignedIn(User) && await UserManager.IsInRoleAsync(UserManager.GetUserAsync(User).Result, "admin2"))
{
   
    <li class="nav-item dropdown show">
        <a class="dropdown-toggle nav-link text-light" href="#" data-toggle="dropdown">Admin</a>
        <div class="dropdown-menu">
            <a class="dropdown-item" asp-area="Admin" asp-controller="Doner" asp-action="Index"><i class="fas fa-box mr-2"></i>View Doners</a>
                       <div class="dropdown-divider"></div>

            <a class="dropdown-item" asp-area="Admin" asp-controller="Requestant" asp-action="Index"><i class="fas fa-users mr-2"></i>View Requestant</a>
            <div class="dropdown-divider"></div>
        </div>
    </li>

}
